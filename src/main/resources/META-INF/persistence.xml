<?xml version="1.0"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
             version="2.0">

<!--
    <persistence-unit name="jpa-objectdb">
        <description>My Persistence Unit</description>
        <provider>com.objectdb.jpa.Provider</provider>
        <properties>
            <property name="javax.persistence.jdbc.url"
                      value="/home/jalbano/objectdbs/testobjectdb.odb" />
            <property name="javax.persistence.jdbc.user" value="admin" />
            <property name="javax.persistence.jdbc.password"
                      value="admin" />
        </properties>
    </persistence-unit>
-->
    <persistence-unit name="jpa-mysql">
        <description>My Persistence Unit for MySQL</description>
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <properties>
            <!-- Driver JDBC para MySQL -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="com.mysql.cj.jdbc.Driver" />

            <!-- URL de conexión a la base de datos MySQL -->
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mysql://localhost:3306/sale_system" />

            <!-- Usuario de la base de datos -->
            <property name="jakarta.persistence.jdbc.user"
                      value="root" />

            <!-- Contraseña de la base de datos -->
            <property name="jakarta.persistence.jdbc.password"
                      value="" />

            <!-- Dialecto de Hibernate para MySQL -->
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.MySQL8Dialect" />

            <!-- Configuración adicional -->
            <property name="hibernate.hbm2ddl.auto" value="update" /> <!-- Usado para crear/actualizar tablas automáticamente -->
            <property name="hibernate.show_sql" value="true" /> <!-- Muestra las consultas SQL en la consola -->
            <property name="hibernate.format_sql" value="true" /> <!-- Formatea el SQL mostrado -->
        </properties>
    </persistence-unit>

</persistence>